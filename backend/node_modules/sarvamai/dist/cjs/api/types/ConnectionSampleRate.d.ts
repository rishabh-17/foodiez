/**
 * Audio sample rate for WebSocket connection parameters. Only 16kHz and 8kHz are supported. 8kHz is only available via connection parameter (not in AudioData messages).
 */
export type ConnectionSampleRate = number;
