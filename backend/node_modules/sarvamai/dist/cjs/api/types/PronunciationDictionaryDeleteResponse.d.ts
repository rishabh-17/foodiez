/**
 * Response returned after deleting a pronunciation dictionary.
 */
export interface PronunciationDictionaryDeleteResponse {
    /** Whether the deletion was successful. */
    success: boolean;
    /** Human-readable status message (e.g. "Dictionary 'p_5cb7faa6' deleted successfully"). */
    message: string;
}
